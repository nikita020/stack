import React, { Component } from 'react';
import {
  View,
} from 'react-native';
import ViewContainer from '../../../components/ViewContainer';

export default class HoldingScreen extends React.Component{
  static navigationOptions = { title: 'Holding' }

  render(){
    return(
      <ViewContainer>
      </ViewContainer>
    )
  }
}
